@using LETS.Models
@model LetsUser
@{
    ViewBag.Title = "My Profile";
    ViewBag.stylesheet = "userProfile.css";
    var message = TempData["PasswordChanged"]?.ToString() ?? "";
}
<div class="form-body">
    <h3 class="form-section">My Profile</h3>

    <hr />

    <div class="alert alert-success @(string.IsNullOrEmpty(message) ? "hide" : "")">
        <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
        <strong>@message</strong>
    </div>

    <div class="form-group">
        <div class="row">
            <div class="col-md-4 information-cols">
                <div class="information-card">
                    @Html.Partial("_Partials/_UserProfileCard")
                </div>
            </div>
            <div id="account_overview_view" class="col-md-8 information-cols show">
                <div class="information-card">
                    @Html.Partial("_Partials/_Overview")
                </div>
            </div>
            <div id="account_settings_view" class="col-md-8 information-cols hide">
                <div class="information-card">
                    @Html.Partial("_Partials/_AccountSettings")
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script type="text/javascript" src="~/Scripts/lets/userProfile.js"></script>
}